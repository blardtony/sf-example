{% extends 'base.html.twig' %}

{% block stylesheets %}
  <style>
  img {
    width: 50%;
  }
  </style>
{% endblock %}

{% block body %}
  <div class="container">
    <h1 class="text-center">Le blog de l'année</h1>
        <div class="row mt-4">
          <div class="col-lg-12 col-md-10">
            <h3 class="title text-center">{{article.title}}</h3>
            <p class=""><small class="text-muted">Fait le {{ article.date.format('d/m/Y') }}</small></p>
            {% if article.image %}
              <div class="text-center">
                <img class="image-article" src="{{article.image}}" alt="">
              </div>
            {% endif %}
            <p class="mt-3 text-justify">{{article.content}}</p>
          </div>
        </div>
  </div>
{% endblock %}
